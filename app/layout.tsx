import type { Metadata, Viewport } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import './globals.css'

export const metadata: Metadata = {
  title: 'Rohit Kumar - Full Stack Developer Portfolio',
  description: 'Full Stack Developer specializing in Java, Spring Boot, Angular, and modern web technologies. View my projects and get in touch for opportunities.',
  keywords: 'Full Stack Developer, Java Developer, Spring Boot, Angular, Portfolio, Web Development',
  authors: [{ name: 'Rohit Kumar' }],
  creator: 'Rohit Kumar',
}

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
  themeColor: '#1e293b',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`${GeistSans.variable} ${GeistMono.variable}`}>
      <head>
        <meta name="mobile-web-app-capable" content="yes" />
        <meta name="apple-mobile-web-app-capable" content="yes" />
        <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
      </head>
      <body className="font-sans antialiased">{children}</body>
    </html>
  )
}
